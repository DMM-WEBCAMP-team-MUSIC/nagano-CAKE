<div class="container">
    <div class="row">
        <div class="col-5">
            <%= attachment_image_tag(@item, :image, :fill, 100, 100, fallback: "no_image.jpg") %>
        </div>
        <div class="col-7">
            <h3><%= @item.name %></h3>
            <p><%= @item.info %></p>
            <h4><%= number_to_currency((@item.price*1.1).floor, unit: "￥", precision: 0) %><small>(税込み)</small></h4>
            <%= form_with do |f| %>
                <%= f.select :quantity, [[1,1],[2,2],[3,3]]%>
                <%= f.submit %>
            <% end %>
        </div>
    </div>
    <div>

    </div>
</div>