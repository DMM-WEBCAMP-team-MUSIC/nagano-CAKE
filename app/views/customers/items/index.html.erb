<div>
  <h1>商品一覧</h1>
  <p>(全<%= @items.count %>件)</p>
</div>
<div>
  <% @items.each do |item| %>
    <div>
      <h5><%= item.name %></h5>
      <h5><%= number_to_currency(item.price, unit: "￥", precision: 0) %></h5>
    </div>
  <% end %>
</div>
