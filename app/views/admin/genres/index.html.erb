<div>
  <h1>ジャンル一覧・追加</h1>
  <%= form_with model: @genre, url: admin_genres_create_path do |f| %>
    <%= f.text_field :name %>
    <%= f.submit "新規登録" %>
  <% end %>
  <table class="table">
    <tr>
      <th>ジャンル</th>
    </tr>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= genre.name %></td>
        <td><%= link_to "編集する", admin_genres_index_path %></td>
      </tr>
    <% end %>
  </table>
</div>
